.env
**/__pycache__/
**/*.pyc
**/.venv/
**/venv/
betedge.db
betedge.db-journal
**/node_modules/
**/dist/
.DS_Store

# API-Football disk cache — regenerable, ~4MB, no need in git
**/.cache/

# Calibrated WC params — regenerated by the pre-tournament calibration job;
# not version-controlled so deploy and dev don't fight over it
model_params_wc.json

# Local IDE state
.vscode/
.idea/
